let a=5;
let name="Name";
let i=0;
let double=0.23;
let result=1/0;
let answer=true;
let no=null;

log.console(a);

log.console(typeof(i));
log.console(typeof(double));
log.console(typeof(result));
log.console(typeof(answer));
log.console(typeof(no));